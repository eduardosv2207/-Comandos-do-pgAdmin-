// comandos para pegar todos os dados da tabela
SELECT * FROM nome_da_tabela;

// comandos para criar tabela
CREATE TABLE nome_da_tabela();

// comandos para criar dados na tabela
INSERT INTO nome_da_tabela(atributos_da_tabela) VALUES (valores_da_tabela);

OBS: os valores só podem ser passados com aspas simples('Eduardo')

CREATE TABLE usuario(
	id serial PRIMARY KEY,
	nome VARCHAR(150) NOT NULL,
	email VARCHAR(150) UNIQUE NOT NULL 
);

CREATE TABLE detalhes_usuario (
	idUsuario int PRIMARY KEY,
	cpf VARCHAR(11) UNIQUE NOT NULL,
	endereco TEXT,
	CONSTRAINT fk_usuarios FOREIGN KEY(idUsuario) REFERENCES usuario(id) on delete cascade 
);

INSERT INTO detalhes_usuario(idusuario, cpf, endereco) VALUES (5, '66666666666', 'casa5')

SELECT * FROM detalhes_usuario

UPDATE detalhes_usuario

SET telefone = '1199999909'

WHERE idusuario = 1

//para deixar na ordem denovo (tabela)
SELECT * FROM pedidos ORDER by id

Atividade:

-- create table autor (
-- 	id serial PRIMARY KEY,
-- 	nome VARCHAR(100) not null
-- )

-- create table livros (
-- 	id serial PRIMARY KEY,
-- 	autor_id int not null, 
-- 	nome_livros VARCHAR(100) not null,
-- 	CONSTRAINT fk_autor FOREIGN KEY(autor_id) REFERENCES autor(id) on delete cascade 
-- )

SELECT * FROM livros

-- INSERT INTO livros(autor_id, nome_livros) VALUES (10 ,'O Alquimista 3')